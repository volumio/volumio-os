# Volumio: Override raspberrypi-sys-mods default to allow radios at boot
# default_state: 0 = radio off (blocked), 1 = radio on (unblocked)
# master_switch_mode: 0 = do nothing, 1 = restore previous, 2 = unblock all
options rfkill default_state=1 master_switch_mode=2
